# Yavapai 13-Signal Swarm (TELEMETRY_SWARM_V5)
# Core
pydantic>=2.7
httpx>=0.27
pyyaml>=6.0
python-dateutil>=2.9

# CLI UX (excellent tables, colors, progress for activation)
rich>=13.7
typer>=0.12

# Web dashboard (glassmorphism live monitor, reuses glass-x patterns)
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9

# Background scheduling for live ticks in dashboard
apscheduler>=3.10

# Optional but recommended for better stats/detectors (pure stdlib fallback exists)
# numpy>=1.26
# pandas>=2.2

# PDF briefing output (SHADOW agent + deployment reports)
fpdf2>=2.7

# Dev / test
pytest>=8.0
